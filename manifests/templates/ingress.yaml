---
# Source: helm for kubernetes/templates/ingress.yaml
apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: jaja                  # Ingress name
  labels:
    app: jaja
spec:
  ingressClassName: nginx   # Optional ingressClass
  rules:
    - host: "jaja.smartops.space"
      http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: jaja               # Links to Service name dynamically
                port:
                  number: 8080  # Links to service port dynamically
